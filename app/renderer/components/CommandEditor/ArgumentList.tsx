/** REACT */
import React, { FC } from 'react';

const ArgumentList: FC = () => {
    return (
        <div/>
    );
};

const Argument: FC = () => {
    return (
        <div/>
    );
};

export default ArgumentList;
